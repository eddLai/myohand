# feat/thumb-decouple — 對掌汙染彎曲的修正（**存檔用，不要合併**）

拇指打直、往小指方向掃過手心時，機器手會一邊正確旋轉、一邊把拇指折起來。
兩根軸各一顆馬達，旋轉那顆是對的，彎曲那顆被連累。這個分支是那件事的
修正嘗試與量測工具。

**這個分支不會被合併，也不該被合併。** 修正本身有效（去掉八成洩漏），
但它會把握拳的動作一起抹掉，見下方「為什麼不合併」。留著是因為量測工具
重寫很花時間，而結論隨時可能需要重新檢視。

## 現況 Status

| | |
|---|---|
| 預設行為 | **與 `main` 逐幀相同**（`OPP_LEAK_TABLE` 是空的，空表不啟用任何修正） |
| 有沒有驅動過機器手 | ❌ 沒有。從頭到尾都沒開啟過 |
| 該不該合併 | ❌ 不該。前提失效，見下 |

## 機制 What actually happens

拇指被手掌擋住的瞬間，模型不是「慢慢失準」，而是整根切換到它學到的先驗。
證據是骨段長度會變：同一根拇指、只是轉了方向，

| 骨段 / 手長 | 拇指張開 | 掃過手心 | 變化 |
|---|---|---|---|
| metacarpal 1-2 | 0.318 | 0.264 | −16.9% |
| proximal 2-3 | 0.350 | 0.389 | +11.3% |
| distal 3-4 | 0.335 | 0.283 | −15.6% |

骨頭不會伸縮，所以中間兩個關節是沿著拇指滑開了 —— 而 `flexion` 完全由
這兩點的夾角算出來。

**這是斷崖，不是斜坡。** 八個掃描角度、每個 40 幀、拇指全程打直：

```
        lite                    full
  對掌    彎曲            對掌    彎曲
   8.8    23.4          -16.7    19.0
  37.0    29.4           38.1    19.3
  66.4    24.6           79.7    20.8   ← 到這裡完全平
  70.4    54.7  ← 斷崖   91.4    47.5   ← 斷崖
  94.1    70.6          107.0    59.1
```

full 掃了 80 度，彎曲讀數只動 1.8 度，然後 4 度之內跳 30 度。平的那一段
同時證明操作者的拇指真的全程打直。

## ⛔ 為什麼不合併 Why this must not be merged

修正假設 `flexion = 真值 + f(opposition)`，也就是**對掌是可信的**。
但拇指折進手心時被擋住的不只彎曲，那個「對掌 99.9 度」本身也是幻覺。

實測（2026-08-07 的 A 組，拇指打直 → 彎 1/3 → 彎 2/3 → 壓平貼掌，
四個姿勢的對掌讀數全在 90 到 100，全部越過門檻）：

| 姿勢 | 沒改 | 開了修正 |
|---|---|---|
| A1 拇指打直 | 1701 | 1850 |
| A2 彎 1/3 | 1434 | 1850 |
| A3 彎 2/3 | 1267 | 1786 |
| A4 壓平貼掌 | 1490 | 1850 |
| **區分幅度** | **434 counts** | **64 counts** |

**真實的彎曲訊號被減掉八成五。而「把拇指壓進手心」正是握拳的動作，
不是邊緣情況。** 用一個常見動作的損壞換一個罕見動作的改善，划不來。

要修好得換觸發條件：門檻不該掛在對掌角度，應該掛在「拇指是否真的被遮住」。
上面量到的骨段失真 ±17% 就是直接證據，且不依賴任何可能同時失真的通道。
那是重新設計，不是調參數。

## 檔案 What is here

| 檔案 | 用途 |
|---|---|
| `camera/hand_mapping.py` | `OPP_LEAK_TABLE` 與 `_opp_leak()` 內插；空表 = 停用 |
| `nn/sweep_probe.py` | 引導操作者停在八個掃描角度並錄影 |
| `nn/fit_table.py` | 把掃描錄影配成修正表 |
| `nn/test_decouple.py` | 留出測試（前三個猜的形狀都被它打掉） |
| `nn/hinge_scan.py` | 找斷崖位置（門檻+斜率那一版留下的） |
| `nn/calib_draw.py`、`nn/sweep_harness.py` | 畫圖與批次跑 |

## 歷史：三個被打掉的形狀

```
線性           洩漏 = 對掌 × 固定係數        ✗ 把一個真實的 31° 彎曲壓成 7°
門檻 + 斜率     超過 70° 之後才線性增加        ✗ 那不是斜坡，是斷崖
查表 + 內插     不猜形狀，直接用量到的八個點    ✓ 去掉 71-77%，但見上方「不合併」
```

⚠️ commit `c319e65` 存下來的是**門檻+斜率那一版**（`OPP_LEAK` / `OPP_ONSET`），
不是查表版 —— 當時 `git add` 之後又改了檔案才 commit，抓到的是暫存區的舊內容。
查表版是後來補上的。

## 已知的界限 What this does not fix

- **斷崖位置只知道落在 66.4 到 70.4 之間**，中間未取樣。補錄 1 分鐘可釘死。
- **洩漏量在不同場次差 9 度**（同一人同一天：44.0 / 34.7 / 約 42）。
  「拇指打直」本身的讀數更差到 20 度。這可能修不掉，所以實際期望是
  去掉八成而非歸零。

## 這個分支的基底

分支從 `dc97d97` / `3d5482d` 長出來，而那兩個 commit 在 2026-08-10 傍晚
重寫作者時已被 `2cc8e6c` / `1d3ddb2` 取代。內容相同、編號不同。
**若有一天真要取用，`cherry-pick` 單一 commit，不要整支合併**，否則會把
重複的 commit 塞回歷史。
